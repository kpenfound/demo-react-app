name: dagger
on:
  push:

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test
        uses: dagger/dagger-for-github@v5
        with:
          args: test
